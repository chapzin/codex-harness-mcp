interface:
  display_name: "Codex Harness MCP"
  short_description: "Contracts, memory, and gates for Codex"
  brand_color: "#2563EB"
  default_prompt: "Use $codex-harness-mcp to query local knowledge, create a small contract, record traces, and run the gate before completion."

policy:
  allow_implicit_invocation: true
